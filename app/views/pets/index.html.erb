<h1>Pets</h1>

<%= link_to 'Add a pet you found!!', new_pet_path, class: 'btn btn-success' %>

<ul>
  <% @pets.each do |pet| %>
    <li><%= link_to pet.name, pet_path(pet) %> - <%= pet.species %></li>
  <% end %>
</ul>
